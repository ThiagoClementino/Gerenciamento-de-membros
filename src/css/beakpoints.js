const size = {
    sm : '480px',
    md : '780px',
    lg : '1024px',
    sg : '1024px',
}

const breakpoints={
    sm: `(max-width: ${size.sm})`,
    md: `(max-width: ${size.md})`,
    lg: `(max-width: ${size.lg})`,
    sg: `(max-width: ${size.sg})`,
}